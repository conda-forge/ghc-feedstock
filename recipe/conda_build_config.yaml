c_stdlib:                  # [not unix]
  - m2w64-sysroot          # [not unix]
c_stdlib_version:          # [not osx]
  - 2.34                   # [linux]
  - 12                     # [not unix]

c_compiler:
  - clang
c_compiler_version:
  - 19                     # [unix and not aarch64]
  - 18                     # [not unix or aarch64]
cxx_compiler:
  - clangxx
cxx_compiler_version:
  - 19                     # [unix and not aarch64]
  - 18                     # [not unix or aarch64]
