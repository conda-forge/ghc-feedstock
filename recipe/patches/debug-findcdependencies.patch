--- a/hadrian/src/Settings/Builders/Cc.hs
+++ b/hadrian/src/Settings/Builders/Cc.hs
@@ -23,6 +23,12 @@ ccBuilderArgs = do
     where
         findCDepExpr depType = do
             output <- getOutput
+            input <- getInput
+            includes <- cIncludeArgs
+            -- Debug output using expr to lift Action into Expr monad
+            expr $ putBuild $ "=== FindCDep input=" ++ input ++ " output=" ++ output
+            expr $ putBuild $ "=== Includes: " ++ show includes
+            -- End debug
             mconcat [ arg "-E"
                     , arg "-MM", arg "-MG"
                     , arg "-MF", arg output
--- a/hadrian/src/Rules/Compile.hs
+++ b/hadrian/src/Rules/Compile.hs
@@ -281,7 +281,14 @@ needDependencies lang context@Context {..} src depFile = do
     parseFile :: FilePath -> Action [String]
     parseFile file = do
         input <- liftIO $ readFile file
+        putBuild $ "=== PARSING " ++ file
+        putBuild $ "=== Contents: " ++ take 500 input ++ "..."
         case parseMakefile input of
-            [(_file, deps)] -> return deps
+            [(_file, deps)] -> do
+                putBuild $ "=== Deps: " ++ show (take 10 deps)
+                let notFound = filter (\d -> d == takeFileName d) deps
+                when (not $ null notFound) $
+                    putBuild $ "=== NOT FOUND (basename only): " ++ show notFound
+                return deps
             _               -> return []
