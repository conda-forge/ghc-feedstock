--- a/rts/sm/Evac.h
+++ b/rts/sm/Evac.h
@@ -24,7 +24,9 @@
 //         take a variable number of arguments will continue to be passed all of
 //         their arguments on the stack.
-#if __GNUC__ >= 2 && (defined(x86_64_HOST_ARCH) || defined(i386_HOST_ARCH))
+// Note: regparm is not valid on ARM64 - detect target architecture, not build architecture
+#if __GNUC__ >= 2 && (defined(x86_64_HOST_ARCH) || defined(i386_HOST_ARCH)) \
+    && !defined(__aarch64__) && !defined(__arm__)
 #define REGPARM1 __attribute__((regparm(1)))
 #else
 #define REGPARM1
