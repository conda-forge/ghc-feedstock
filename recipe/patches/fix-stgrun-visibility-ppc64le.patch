--- a/rts/StgCRunAsm.S
+++ b/rts/StgCRunAsm.S
@@ -32,7 +32,10 @@
 	.section	".text"
 	.align 2
         .globl StgRun
+#if !defined(__PIC__)
+/* Only hide symbol in static libraries, not shared libraries (-fPIC) */
         .hidden StgRun
+#endif
         .type StgRun,@function
 StgRun:
         .localentry StgRun,.-StgRun
