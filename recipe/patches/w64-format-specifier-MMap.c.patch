--- a/rts/linker/MMap.c
+++ b/rts/linker/MMap.c
@@ -124,8 +124,10 @@ static void *allocateBytes(void* baseAddr, void *endAddr, size_t sz, size_t *re

         if ((info.State & MEM_FREE) == MEM_FREE) {
-            IF_DEBUG(linker_verbose, debugBelch("Free range at 0x%p of %zu bytes\n",
-                                        info.BaseAddress, info.RegionSize));
+            // Windows MinGW doesn't support %zu format specifier properly
+            // Use %llu instead and cast to unsigned long long
+            IF_DEBUG(linker_verbose, debugBelch("Free range at 0x%p of %llu bytes\n",
+                                        info.BaseAddress, (unsigned long long) info.RegionSize));

             if (info.RegionSize >= sz) {
                 if (info.AllocationBase == 0) {
