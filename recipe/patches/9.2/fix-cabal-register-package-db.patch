From: conda-forge
Subject: [PATCH] Use gen-pkg-config + ghc-pkg register for Stage0 packages

When using a different bootstrap GHC version (e.g., 9.6.7 to build 9.2.8),
Cabal's register command doesn't reliably register packages to the Stage0
package database. The issue is that 'cabal register' without explicit
package-db specification may register to the wrong database.

The fix:
1. For Stage0, use 'cabal register --gen-pkg-config=<path>' to generate
   the .conf file to a TEMP location (NOT in the package db)
2. Then use 'ghc-pkg register' to register it to the correct Stage0 db

Key insight: If we write the .conf file directly to the package db directory,
ghc-pkg will see it and say "already exists" even with --force. We must
write to a separate location first.

--- a/b/hadrian/src/Hadrian/Haskell/Cabal/Parse.hs	2025-12-16 09:30:00.274183813 -0600
+++ b/hadrian/src/Hadrian/Haskell/Cabal/Parse.hs	2025-12-16 09:34:20.963113504 -0600
@@ -11,7 +11,7 @@
 -----------------------------------------------------------------------------
 module Hadrian.Haskell.Cabal.Parse (
     parsePackageData, resolveContextData, parseCabalPkgId, configurePackage,
-    buildAutogenFiles, copyPackage, registerPackage
+    buildAutogenFiles, copyPackage, registerPackage, genPkgConfig
     ) where
 
 import Data.Bifunctor
@@ -193,6 +193,21 @@
     traced "cabal-register" $
         C.defaultMainWithHooksNoReadArgs C.autoconfUserHooks gpd
             [ "register", "--builddir", ctxPath, v ]
+
+-- | Generate a package config file for the 'Package' without registering.
+-- This is useful when we need to register to a specific package database
+-- using ghc-pkg rather than cabal's default registration.
+genPkgConfig :: Context -> FilePath -> Action ()
+genPkgConfig context@Context {..} confFile = do
+    putProgressInfo $ "| Generate pkg config " ++ quote (pkgName package)
+    ctxPath <- Context.contextPath context
+    gpd <- pkgGenericDescription package
+    verbosity <- getVerbosity
+    let v = if verbosity >= Loud then "-v3" else "-v0"
+    traced "cabal-gen-pkg-config" $
+        C.defaultMainWithHooksNoReadArgs C.autoconfUserHooks gpd
+            [ "register", "--builddir", ctxPath, "--gen-pkg-config=" ++ confFile, v ]
+
 -- | What type of file is Main
 data MainSourceType = HsMain | CppMain | CMain
 
--- a/b/hadrian/src/Rules/Register.hs	2025-12-16 09:30:00.274183813 -0600
+++ b/hadrian/src/Rules/Register.hs	2025-12-16 09:35:49.827430203 -0600
@@ -142,8 +142,31 @@
             need [path -/- "include/ghc-gmp.h"]
 
     -- Copy and register the package.
+    -- Note: For Stage0 with different bootstrap versions, Cabal.registerPackage
+    -- doesn't reliably register to the correct package database. We generate
+    -- the .conf file using --gen-pkg-config, then register with ghc-pkg directly.
     Cabal.copyPackage context
-    Cabal.registerPackage context
+
+    case stage of
+        Stage0 -> do
+            -- Generate .conf file to a TEMP location (NOT in the package db directory)
+            -- Then register it explicitly with ghc-pkg to our Stage0 database.
+            -- We MUST write to a temp location first because if we write directly
+            -- to the package db, ghc-pkg will see the file and say "already exists".
+            ctxPath <- contextPath context
+            pkgDbPath <- packageDbPath stage
+            pkgId <- pkgIdentifier package
+            let tempConfFile = ctxPath -/- "build" -/- pkgId <.> "conf"
+            Cabal.genPkgConfig context tempConfFile
+            -- Now register the generated .conf file with ghc-pkg
+            putProgressInfo $ "| Register package " ++ quote (pkgName package) ++ " (via ghc-pkg)"
+            ghcPkgPath <- builderPath (GhcPkg Init stage)
+            verbosity <- getVerbosity
+            let verbosityFlag = if verbosity < Chatty then ["-v0"] else []
+            cmd_ [ghcPkgPath] (["--no-user-package-db", "--global-package-db", pkgDbPath
+                              , "register", "--force"] ++ verbosityFlag ++ [tempConfFile])
+        _ ->
+            Cabal.registerPackage context
 
     -- We declare that this rule also produces files matching:
     --   - <root>/stage<N>/lib/<arch>-<os>-ghc-<version>/*libHS<pkgid>*
